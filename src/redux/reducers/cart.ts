import { Reducer } from 'redux'

const initialState: any = {
  
}

export const cartReducer: Reducer<any> = (state = initialState, action): any => {

  switch(action.type){
    
    default:
      return state
  }
}